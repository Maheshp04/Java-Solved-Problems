Write a program to print all prime numbers in the given range (inclusive). A number is considered prime if it has only two factors: 1 and itself.
Input Format

Two space-separated integers: start end
Constraints

1 ≤ start < end ≤ 10,000
Output Format

Print all prime numbers in the range separated by space.
If no primes found, print No prime numbers.
Sample Input 0

1 10
Sample Output 0

2 3 5 7
Sample Input 1

14 16
Sample Output 1

No prime numbers

import java.io.*;
import java.util.*;

public class Solution {

    public static void main(String[] args) {
        Scanner sc=new Scanner(System.in);
        int m=sc.nextInt();
        int n=sc.nextInt();
        boolean found=false;
        for(int i=m;i<=n;i++){
            if(i<=1)continue;
            boolean isPrime=true;
            for(int j=2;j<=i/2;j++){
                if(i%j==0){
                    isPrime=false;
                    break;
                }
            }
            if(isPrime){
                System.out.print(i+" ");
                found=true;
            }
        }
        if(!found){
            System.out.println("No prime numbers");
        }
        sc.close();
    }
}
